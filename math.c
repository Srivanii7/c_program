#include<stdio.h>
#include<math.h>
int main(){
    // sqrt
    printf("%f",sqrt(10));
    // pow
    printf("\n%f",pow(2,3));
    // round-ceil,floor
    printf("\n%f",ceil(2.5));
    printf("\n%f",floor(2.5));
}